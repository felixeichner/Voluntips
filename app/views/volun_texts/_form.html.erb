<hr><br>

<%= form_with(model: volun_text, local: true) do |form| %>
  <% if volun_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(volun_text.errors.count, "error") %> prohibited this volun_text from being saved:</h2>

      <ul>
      <% volun_text.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="content-container">
    <div class="form-group">
      <strong><%= form.label :title %></strong>
      <%= form.text_field :title, id: :volun_text_title, class: "form-control" %>
    </div>

    <div class="form-group">
      <strong><%= form.label :body %></strong>
      <%= form.text_area :body, id: :volun_text_body, rows: 15, class: "form-control" %>
    </div>

    <div class="actions">
      <%= form.submit current_action_label, class: "btn btn-designed" %>
    </div>
  </div>
  <br>
<% end %>
<%= link_to 'Zurück', url_for(:back), class: "btn btn-designed" %>